[package]
name = "mac-audio-keepalive"
version = "0.1.0"
edition = "2021"

[dependencies]
coreaudio-sys = "0.2"
libc = "0.2"
signal-hook = "0.3"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
